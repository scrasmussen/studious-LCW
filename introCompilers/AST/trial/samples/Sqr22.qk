class Pt(x: Int, y: Int) {
    this.x = x;
    this.y = y; 
    def PRINT( ):Pt {
       "( ".PRINT(); 
       this.x.PRINT(); 
       ", ".PRINT();
       ")".PRINT(); 
     }
     def PLUS(other: Pt) {
         return Pt(this.x+other.x, this.y+other.y); 
     }
  }
     pt1 = Pt(x,y); 
     pt1.PRINT(); 
     pt2 = Pt(y,x); 
     pt2.PRINT();
     pt3 = pt1 + pt2; 
     pt3.PRINT(); 
