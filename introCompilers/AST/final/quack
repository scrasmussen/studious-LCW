#!/bin/bash
CC=gcc
CFLAGS=-w

quackCompiler $1
FILENAME=$(basename "$1")
FILENAME="${FILENAME%.*}"
echo ${CC} Builtins.o ${FILENAME}.c ${CFLAGS} -o ${FILENAME}
${CC} Builtins.o ${FILENAME}.c ${CFLAGS} -o ${FILENAME}
echo ./${FILENAME}
./${FILENAME}
